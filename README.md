# Goutham Gourabathuni — Portfolio

Modern, responsive one-page portfolio built with Next.js 14 (App Router) and Tailwind CSS. Ready to deploy on Vercel.

## Local development

```
# install deps
pnpm i # or npm i / yarn

# start dev server
pnpm dev # or npm run dev / yarn dev
```

Open http://localhost:4000

## Content

Edit structured resume data in `content/resume.ts`.

## Deploy to Vercel

1. Push this repo to GitHub.
2. Import the repository in Vercel and deploy with default settings.

No extra configuration is required.
